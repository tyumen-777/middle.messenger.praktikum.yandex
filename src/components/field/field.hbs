<input
  class="field__input"
  type="{{ type }}"
  name="{{ name }}"
  id="{{ name }}"
  value="{{ value }}"
  placeholder="{{ name }}"
  autocomplete="false"
  accept="{{accept}}"
  {{#if multiple}}
  multiple
  {{/if}}
/>
